package com.puskal.oniondiagram

import android.graphics.Color

object MyUtils {

    /**
     * default RGB color list. If you not define color for onion, this color list will use
     */
    val ColorList = intArrayOf(
            Color.rgb(255, 102, 102), Color.rgb(102, 255, 102), Color.rgb(102, 178, 255),
            Color.rgb(255, 102, 178), Color.rgb(255, 255, 153),Color.rgb(178,102,255)
    )
}